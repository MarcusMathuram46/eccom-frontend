// Spinner.jsx
import React from 'react';
import '../styles/Spinner.css'; // Add spinner styles

const Spinner = () => {
  return (
    <div className="spinner"></div>
  );
};

export default Spinner;
